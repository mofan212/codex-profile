# 1. 代码组织

## 1.1 类与职责

- 一个类只承担一个清晰职责，类名和后缀应直接表达用途；复杂命名、角色后缀、`Dto` / `Dao` 边界和变量常量命名优先使用 `$java-naming` 判断
- Service、Domain、Controller、Mapper、Dto、Dao 等分层代码优先贴合当前项目已有调用链，不因为新增需求扩大接口行为面
- 任务类、调度类、入口类、Controller 或 Service 编排方法优先只做流程编排；复杂对象构造、字段填充和分组转换下沉到专门构造类或工厂类
- 工具类通常使用 `final` 类加私有构造方法，方法以 `public static` 为主；如果当前项目已有其他约定，跟随项目约定
- 需要封装构造过程的类可以使用私有构造方法加静态工厂方法，工厂方法命名优先贴合当前项目习惯，例如 `from`、`of`、`create`、`build`
- 只有存在真实 `is-a` 语义时才使用继承；不要为了复用静态工具能力引入语义不成立的继承
- Spring 组件保持轻量，使用当前项目已有注入方式和 Bean 组织方式，不主动混用不同注入风格
- Spring Bean、基础设施协作者或稳定运行时依赖不应作为普通业务参数在多层方法间传递；优先注入到当前组件或专门协作者中，方法参数只保留本次调用真正变化的业务数据
- 修改 MyBatis 相关代码时，同步核对 Mapper 接口、XML、参数名、返回映射和调用方，避免只改一侧

## 1.2 文件头与类注释

- 新建 Java 类时，添加文档注释，并写入作者 `@author` 和日期时间 `@date` 信息：
  - `@author`：运行 [resolve_java_author.py](../scripts/resolve_java_author.py) 获取取值；脚本输出即为最终可用值，不要复制非当前用户类注释中的 `@author` 信息
  - `@date`：优先跟随同包或同模块已有标签和格式；没有既有模式时使用 `@date yyyy/MM/dd HH:mm`
- 如果已有类注释，不修改原来的作者、日期或格式
- 类注释可以简洁；当类名和上下文已能说明职责时，不强行加冗长说明

## 1.3 导入与格式

- 不使用通配符导入
- 不要用全限定类名替代正常导入
- 除非存在同名类冲突，正文中引用类型时，统一在文件头部使用 `import`
- 导入顺序、缩进和换行跟随当前文件、同包类和项目 IDE 默认整理结果
- 多参数方法调用换行时，保持可读性和局部一致
- 避免无意义格式改动；只整理被修改文件必要范围

## 1.4 兼容入口

- 新代码不要依赖 `@Deprecated` 类、方法或职责偏移的历史工具类，优先改为依赖非废弃的稳定入口
- 需要兼容旧调用点时，让旧类静态转发到新的稳定入口，不要让新代码反向依赖旧兼容类
- 必须复用废弃类中的常量、判断逻辑或命名规则时，先抽取到非废弃的稳定领域工具类，新代码依赖稳定入口，旧类只保留兼容转发
- 旧依赖只是兼容入口且只做静态转发时，可以保留并说明依据

# 2. 命名习惯

## 2.1 业务命名

- 名称优先跟随当前项目、同包类和直接调用链
- 避免抽象、空泛或只描述技术实现的命名
- 复杂角色命名、包层级和类后缀使用 `$java-naming` 判断
- `DTO` / `DAO` 边界、布尔命名、集合命名和重命名任务使用 `$java-naming` 判断
- 历史 `DTO`、`DAO`、`BO`、`VO`、`DO`、`Entity`、`Enum` 等后缀跟随当前项目，不主动更换领域模型命名体系

# 3. 方法实现

## 3.1 控制流

- 控制流优先使用卫语句返回结果，减少主流程嵌套
- 先处理空值、空集合、不满足条件和异常分支，再进入主流程
- 新增分支逻辑时，优先使用清晰的条件表达、枚举或策略映射；只有复杂度真实增长时才引入设计模式
- 包含副作用、日志、异常处理、多步状态变更或复杂分支时，保留命令式写法
- 不为 Stream、Optional、Lambda 风格牺牲可读性

## 3.2 集合操作

处理集合时，优先使用 Apache Commons Collections4 的工具类完成空值兜底、判空、过滤、转换等通用操作；项目已有等价封装时，优先遵循项目既有封装。

对简单集合逻辑，优先使用 Stream 表达查询、过滤、映射、匹配、聚合等意图，避免展开成冗长的 `for` 循环、临时变量和多层 `if`。

| trigger | requirement | forbidden |
| --- | --- | --- |
| 集合可能为 `null`，但后续仍要遍历或调用 `.stream()` | 优先使用 `ListUtils.emptyIfNull(...)`、`CollectionUtils.emptyIfNull(...)` 等 Commons Collections4 工具类归一化为空集合 | 手写重复的空值和空集合判断，除非提前返回能显著提升可读性 |
| 简单筛选、映射、取首个、任意匹配、全部匹配 | 优先使用 Stream 的 `filter`、`map`、`findFirst`、`anyMatch`、`allMatch` 等表达主流程 | 为简单查询写命令式循环、`continue` 和临时变量 |
| Stream 中只是调用已有无参方法或静态方法 | 优先使用方法引用 | 写成无必要的 Lambda |
| Stream 条件需要组合外部变量、工具类调用或业务判断 | 使用 Lambda，并保持条件短而直接 | 为追求方法引用强行拆出低价值私有方法 |
| 逻辑包含复杂分支、副作用、日志、异常处理、多步状态变更 | 保留命令式写法，优先保证控制流清晰 | 为了 Stream 风格压缩复杂流程 |

## 3.3 参数收敛

- 方法参数过多时，先按业务生命周期分组，例如批次级、请求级、上下文级、当前执行项；只有属于同一概念的数据才收敛为值对象
- 不要为了减少参数把语义不同的对象硬合并。当前执行点必须显式表达的对象，例如当前任务、当前节点、当前实体或当前状态，应该保留在方法签名中
- 不要为了把多个参数包起来而引入空泛对象名，例如没有稳定领域语义的 `Spec`、`Options`、`Config`、`Context`。如果没有清晰名称，通常说明还不该抽
- 抽出的值对象应让调用方更容易理解执行边界；如果只是把散乱参数藏到对象里而业务语义没有变清楚，应保持直接参数

## 3.4 空值与默认值

- 不添加无必要的防御性判断；先确认上游调用、接口契约、构造器、注入生命周期或当前方法内控制流是否已保证对象非空、集合非空或字段已初始化
- 上游已完成 `null`、空集合、空字符串或状态校验时，下游直接使用已保证的数据，不重复写 `if (x == null)`、`CollectionUtils.isEmpty(...)`、`StringUtils.isBlank(...)` 之类兜底
- 对外部请求、数据库历史脏数据、第三方接口返回、反射、JSON 反序列化或可空接口契约，允许在边界入口补充必要判断，并让后续内部方法接收已归一化的数据
- 空值、集合、Map、字符串判断优先使用当前项目已有工具类和写法
- 返回空结果时优先使用 `Collections.emptyList()`、`Collections.emptySet()`、`Collections.emptyMap()` 等不可变空集合
- 可缺省单值可以返回 `Optional<T>`，但必须与现有接口契约兼容
- 不为了消除 `null` 而改变调用方可观察行为

## 3.5 异常和日志

- 异常处理优先复用项目已有业务异常类型、错误码、异常抛出工具或统一异常处理机制，不随意引入新的异常体系
- 可恢复的工具方法可以记录日志后返回空结果；业务失败应按项目统一异常机制抛出
- 日志模板使用占位符，不拼接字符串
- 只记录异常、外部依赖失败、关键状态变化和定位问题所需上下文
- 不为每个分支打日志
- 避免日志淹没有效代码
- 对兼容性要求明确的行为，保留原接口语义；不得为了局部代码简洁改变调用方可观察行为

# 4. 注释风格

## 4.1 通用规则

- 注释解释为什么这样处理，而不是重复代码做了什么
- 非必要，注释末尾不添加句号
- 中文与英文、数字、常规单位、行内代码之间留一个半角空格
- 中文语境使用全角标点，中文标点前后不加空格；完整英文句子内部保留英文半角标点
- 中文引用内容需要使用弯引号时，使用直角引号（`「」` 对应 `“”`，`『』` 对应 `‘’`）
- 专有名词按官方或通行写法书写
- 不添加装饰性注释，不把每一行都翻译成中文
- 类、字段、常量、静态变量和方法声明前的说明使用文档注释，不得使用 `//` 单行注释
- 方法内部的一句业务意图说明可以使用 `//` 单行注释；同一段说明超过一行时使用 `/* ... */` 块注释

## 4.2 文档注释

- 非显而易见的 public 方法添加文档注释，包含必要的 `@param`、`@return`、`@apiNote`
- 私有方法只有在递归、反射、映射算法等不直观时才补充文档注释
- 字段、常量和复杂配置可以用短文档注释说明业务含义

正例 - 字段说明使用文档注释：

```java
/**
 * 是否启用历史兼容模式
 */
private boolean legacyCompatible;
```

反例 - 字段说明使用单行注释：

```java
// 是否启用历史兼容模式
private boolean legacyCompatible;
```

## 4.3 单行注释

- 对复杂算法，优先在关键分支前写一句说明业务意图的注释
- 多个连续 `//` 单行注释表达同一段说明时，改为 `/* ... */` 块注释

正例 - 连续说明使用块注释：

```java
/*
 * 兼容历史任务没有批次号的场景
 * 这里只补默认批次，不改变原始状态
 */
repairLegacyBatch(task);
```

反例 - 同一段说明拆成多个连续单行注释：

```java
// 兼容历史任务没有批次号的场景
// 这里只补默认批次，不改变原始状态
repairLegacyBatch(task);
```

正例 - 短注释末尾不加句号：

```java
// 保留空结果，避免影响旧调用方
return Collections.emptyList();
```

反例 - 短注释末尾添加句号：

```java
// 保留空结果，避免影响旧调用方。
return Collections.emptyList();
```

正例 - 中文弯引号使用 `「」`：

```java
// 对「未开始」状态保留原排序
sortPendingItems(items);
```

反例 - 中文语境使用 `“”`：

```java
// 对 “未开始” 状态保留原排序
sortPendingItems(items);
```

# 5. 常见代码形态

## 5.1 使用原则

本节示例只表达常见结构，不要求照抄类名、后缀、依赖或字段类型。优先沿用当前项目已有形态。

| 形态 | 适用场景 | 注意事项 |
| --- | --- | --- |
| 工具类 | 无状态、可复用、与具体业务流程弱绑定的纯函数能力 | 使用 `final` 类和私有构造方法；不要注入 Spring Bean；不要把业务流程塞进工具类 |
| 构造/转换协作者 | 对象构造、字段填充、分组转换或多步骤映射逻辑明显膨胀 | 类名和后缀跟随项目习惯；不要默认使用 `Support` 后缀；只在确实降低主流程复杂度时抽出 |
| 辅助协作者 | 需要依赖注入或承载多处重复协作细节 | 按 `$java-naming` 判断类名和职责包；不要把 Spring Bean 依赖作为参数传给静态工具方法 |
| 流程编排类 | 调度、入口、批处理、Controller 或 Service 中串联多个步骤 | 保持轻量，只组织流程；复杂转换、校验或构造下沉到稳定协作者 |
| 静态工厂方法 | 构造参数需要校验、归一化或表达业务语义 | 方法名跟随项目习惯，例如 `from`、`of`、`create`、`build`；不要为了样式统一强行引入 |

# 6. 单测风格

## 6.1 测试框架

- 优先沿用当前模块已有测试框架。模块使用 JUnit4 就按 JUnit4 写；模块使用 JUnit5 就按 JUnit5 写
- 断言优先使用 AssertJ；如果项目已有统一断言工具，跟随项目
- Mock 使用 Mockito 或当前项目已有 Mock 工具，保持测试可读、稳定、聚焦

## 6.2 测试命名

- 测试方法名应表达被测方法、业务场景和预期结果
- 每个测试聚焦一个分支或一个异常场景
- 测试内可以用短中文注释标记业务场景，但不要写成长篇说明
- 测试重点覆盖新增行为、边界分支、异常路径和已有行为兼容性

# 7. 代码评审清单

## 7.1 提交前检查

- 是否先读过同包或同类型样本，并保持局部一致
- 是否避免过度设计，只解决当前需求
- 是否避免了上游已保证条件下的重复空值、空集合和空字符串判断
- 是否优先使用卫语句降低主流程嵌套
- 是否只记录必要日志，避免日志淹没有效代码
- 是否复用项目已有工具类、异常体系、领域模型和接口契约
- 是否保留已有作者注释；新建类作者是否先遵循用户或仓库约定，无法确认时才使用 Git 用户名
- Java 注释是否符合中文文案排版要求
- 是否检查了相关配置、Mapper XML、调用方和测试覆盖
- 是否补充了与行为变化相匹配的聚焦单测
- 如果无法运行测试或编译，是否说明原因和已完成的静态核对范围
