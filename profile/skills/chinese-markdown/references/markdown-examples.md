# 1. Markdown 示例

# 2. 行内语法

```markdown
正确：运行 `example --help` 查看可用参数
错误：运行`example --help`查看可用参数

正确：不要引入 `Spec`、`Options`、`Config`。
错误：不要引入 `Spec` 、 `Options` 、 `Config` 。

正确：配置项包括 `timeout`、`retry`；默认值使用 `false`。
错误：配置项包括`timeout`、`retry`；默认值使用`false`。

正确：请 **先确认范围** 再继续修改
错误：请**先确认范围**再继续修改

正确：参考 [使用说明](guide.md) 完成配置
错误：参考[使用说明](guide.md)完成配置

正确：状态为 <span class="ok">通过</span>，继续执行
错误：状态为<span class="ok">通过</span> ，继续执行

正确：结果（<span class="ok">通过</span>）
错误：结果（ <span class="ok">通过</span> ）

正确：保留模板变量 `$VALUE$`、Shell 变量 `$PATH`、JSONPath `$` 和正则行尾 `$`
错误：保留模板变量 `VALUE`、Shell 变量 `PATH`、JSONPath 和正则行尾
```

# 3. 标题层级

正确示例：

```markdown
# 1. 背景
## 1.1 当前问题

# 2. 方案
## 2.1 实施步骤
```

既有文档使用 0 起始时，保留编号基线：

```markdown
# 0. 前言
## 0.1 背景

# 1. 正文
## 1.1 实施步骤
```

错误示例：

```markdown
# 1. 项目说明
## 1.1 背景
## 1.2 当前问题
## 1.3 方案
## 1.4 实施步骤
```

更细层级优先使用引用块：

```markdown
## 2.1 实施步骤

> 准备配置文件

准备配置文件的内容...

> 运行校验命令

运行校验命令的内容...
```

不要新增 3 级或更深标题：

```markdown
## 2.1 实施步骤
### 2.1.1 准备配置文件

准备配置文件的内容...
```

# 4. 尾部标点

正确示例：

```markdown
- 标题编号连续且层级正确
- 表格内文字末尾没有多余标点
- 命令和编码规则：

    | trigger | requirement |
    | --- | --- |
    | 执行 PowerShell 文件读写命令 | 显式附加 `-Encoding UTF8` |

1. 确认目标文档范围
2. 修正标题编号
3. 检查表格尾部标点

1. 确认目标文档范围；
2. 修正标题编号；
3. 检查表格尾部标点。

| trigger | action |
| --- | --- |
| 维护 Skill 文档 | 检查列表和表格尾部标点 |
```

错误示例：

```markdown
- 标题编号连续且层级正确。
- 表格内文字末尾没有多余标点；

1. 确认目标文档范围。
2. 修正标题编号：
3. 检查表格尾部标点；

| trigger | action |
| --- | --- |
| 维护 Skill 文档。 | 检查列表和表格尾部标点； |
```

# 5. 省略号

目标文档采用半角三点风格时：

```markdown
正确：等待任务完成...
错误：等待任务完成……
错误：等待任务完成......

正确：函数签名为 `fn(...args)`
错误：函数签名为 `fn(…args)`
```
