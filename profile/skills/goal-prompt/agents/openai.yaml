interface:
  display_name: "Goal Prompt"
  short_description: "根据当前对话生成可复制到新会话并执行任务的 Goal 提示词"
  default_prompt: "使用 $goal-prompt 根据当前对话生成可复制到新会话的 Goal 提示词。"

policy:
  allow_implicit_invocation: false
