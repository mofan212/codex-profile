---
name: gen-goal-prompt
description: 根据当前对话生成可复制到新会话的 Goal 提示词。
---

根据当前对话，**仅用 Markdown 围栏代码块** 输出一段可直接复制到新会话、用于完成已讨论任务的自包含提示词：

- 必须给出具体 Goal，并要求新会话创建后持续执行直至完成
- 保留已确认的目标、范围、约束和验收要求
- 允许按需启用 subagents
- 不得依赖当前会话上下文
- 使用紧凑的提示词结构，避免单次回复超过长度限制而被截断
